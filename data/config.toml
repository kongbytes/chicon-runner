# This file should always be treated as sensitive (400 permissions at least)

[workspace]
path = "/home/corentin/workspace/chicon-runner/workspace"
cache_limit = "20" # Mb
ssh_clone_key = "/home/corentin/.ssh/chicon-runner"

[scheduler]
base_url = "localhost:3000"
token = "a95bdaa3-00c9-4b4a-9593-8e6625430273"
retry_period = 5 # Seconds
retry_scale_factor = 1.25
retry_scale_limit = 30 # Seconds

[container]
namespace = "kb"
