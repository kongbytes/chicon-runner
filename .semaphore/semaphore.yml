version: v1.0
name: Rust

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2204

blocks:
  - name: Test release
    task:
      jobs:
        - name: Build & test
          commands:
            - checkout
            - rustc --version
            - cargo build --verbose
